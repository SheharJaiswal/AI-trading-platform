import {ComponentFixture,TestBed} from '@angular/core/testing';import {of,throwError} from 'rxjs';import {BacktestComponent} from './backtest.component';import {TradingApiService} from './core/api/trading-api.service';
describe('BacktestComponent',()=>{let fixture:ComponentFixture<BacktestComponent>;let component:BacktestComponent;let api:jasmine.SpyObj<TradingApiService>;const response:any={runId:'run-1',status:'completed',simulationLabel:'HISTORICAL_SIMULATION_ONLY',result:{startingCash:10000,endingCash:10100,returnPercent:1,maxDrawdownPercent:2,trades:[],equityCurve:[{timestamp:'2026-01-01T00:00:00Z',cash:10000,positionValue:0,equity:10000}],riskEvents:[],simulationOnly:true}};beforeEach(async()=>{api=jasmine.createSpyObj('TradingApiService',['backtest']);await TestBed.configureTestingModule({imports:[BacktestComponent],providers:[{provide:TradingApiService,useValue:api}]}).compileComponents();fixture=TestBed.createComponent(BacktestComponent);component=fixture.componentInstance});it('renders persisted simulation result and provenance state',()=>{api.backtest.and.returnValue(of(response));component.submit(new Event('submit'));fixture.detectChanges();expect(component.result?.runId).toBe('run-1');expect(fixture.nativeElement.textContent).toContain('Dataset validation passed');expect(fixture.nativeElement.textContent).toContain('HISTORICAL SIMULATION ONLY')});it('surfaces server validation errors',()=>{api.backtest.and.returnValue(throwError(()=>({error:{message:'No historical data is available'}})));component.submit(new Event('submit'));expect(component.message).toContain('No historical data')});});
