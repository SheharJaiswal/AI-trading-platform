import {CommonModule} from '@angular/common';import {Component,inject} from '@angular/core';import {TradingApiService} from './core/api/trading-api.service';import {BacktestRequest,BacktestResponse} from './core/api/trading-api.models';
@Component({selector:'app-backtest',standalone:true,imports:[CommonModule],template:`<section class="backtest"><header><div><p class="eyebrow">RESEARCH / BACKTEST</p><h1>Historical Backtest</h1><p class="muted">Deterministic replay of historical candles. This is simulation only and cannot submit orders.</p></div><span class="simulation">HISTORICAL SIMULATION ONLY</span></header><form (submit)="submit($event)"><label>Symbol<input [value]="symbol" (input)="symbol=$any($event.target).value" required></label><label>Interval<input [value]="interval" (input)="interval=$any($event.target).value" required></label><label>Start<input type="datetime-local" [value]="start" (input)="start=$any($event.target).value" required></label><label>End<input type="datetime-local" [value]="end" (input)="end=$any($event.target).value" required></label><label>Starting cash<input type="number" [value]="startingCash" (input)="startingCash=+$any($event.target).value" min="1" required></label><label>Quantity / trade<input type="number" [value]="quantity" (input)="quantity=+$any($event.target).value" min="1" required></label><label>Fee rate<input type="number" [value]="feeRate" (input)="feeRate=+$any($event.target).value" min="0" step="0.0001" required></label><label>Slippage (bps)<input type="number" [value]="slippage" (input)="slippage=+$any($event.target).value" min="0" step="0.1" required></label><button type="submit" [disabled]="loading">{{loading?'Running…':'Validate & run simulation'}}</button></form>@if(message){<div class="notice">{{message}}</div>}@if(result){<section class="results"><div class="provenance"><strong>Dataset validation passed</strong><span>Historical candles were ordered and validated by the server; source/provenance is retained with the persisted dataset.</span><span>Run ID: {{result.runId}}</span></div><h2>Simulation result</h2><div class="metrics"><div><span>Ending equity</span><strong>{{result.result.endingCash|number:'1.2-2'}}</strong></div><div><span>Return</span><strong>{{result.result.returnPercent|number:'1.2-2'}}%</strong></div><div><span>Max drawdown</span><strong>{{result.result.maxDrawdownPercent|number:'1.2-2'}}%</strong></div><div><span>Approved trades</span><strong>{{result.result.trades.length}}</strong></div><div><span>Risk events</span><strong>{{result.result.riskEvents.length}}</strong></div><div><span>Win / loss</span><strong>{{result.result.winCount??0}} / {{result.result.lossCount??0}}</strong></div></div><h3>Equity curve</h3><div class="curve">@for(point of result.result.equityCurve;track point.timestamp){<div class="point" [style.height.px]="curveHeight(point.equity)" [title]="point.timestamp+' '+point.equity"></div>}</div><p class="muted">Each point represents end-of-candle marked equity; no future candle is used for an earlier decision.</p><h3>Simulated trade ledger</h3>@if(result.result.trades.length){<table><thead><tr><th>Time</th><th>Side</th><th>Qty</th><th>Price</th><th>Fee</th><th>Risk</th></tr></thead><tbody>@for(trade of result.result.trades;track trade.timestamp+trade.side+trade.quantity){<tr><td>{{trade.timestamp|date:'short'}}</td><td>{{trade.side}}</td><td>{{trade.quantity}}</td><td>{{trade.price|number:'1.2-2'}}</td><td>{{trade.fee|number:'1.2-2'}}</td><td>{{trade.riskDecision}}</td></tr>}</tbody></table>}@else{<p class="empty">No trades were approved; risk decisions remain recorded below.</p>}<h3>Risk decision ledger</h3><table><thead><tr><th>Time</th><th>Decision</th><th>Cash</th><th>Requested qty</th><th>Reason</th></tr></thead><tbody>@for(event of result.result.riskEvents;track event.timestamp+event.decision){<tr><td>{{event.timestamp|date:'short'}}</td><td>{{event.decision}}</td><td>{{event.availableCash|number:'1.2-2'}}</td><td>{{event.requestedQuantity}}</td><td>{{event.reason||'—'}}</td></tr>}</tbody></table></section>}<aside><strong>Safety boundary.</strong> Backtests never call paper execution, never mutate the live portfolio, and AI cannot authorize simulated trades.</aside></section>`,styles:[`.backtest{max-width:1120px;margin:2rem auto;padding:1.5rem}.eyebrow{font-size:.72rem;letter-spacing:.08em}.muted{color:#687386}.simulation{padding:.4rem .7rem;border:1px solid currentColor;font-size:.72rem}header{display:flex;justify-content:space-between;gap:1rem}form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1rem;margin:1.5rem 0}label{display:grid;gap:.35rem;font-size:.85rem}input{padding:.65rem;border:1px solid #ccd2dc;border-radius:7px}button{grid-column:1/-1;padding:.75rem;border:0;border-radius:7px}.notice,.provenance,aside{margin:1rem 0;padding:1rem;border:1px solid #d9dee7;border-radius:9px}.provenance{display:grid;gap:.35rem}.metrics{display:grid;grid-template-columns:repeat(6,1fr);gap:.7rem}.metrics div{border:1px solid #ddd;padding:.8rem}.metrics span,.metrics strong{display:block}.metrics strong{margin-top:.3rem}.curve{height:180px;display:flex;align-items:end;gap:2px;padding:1rem 0;border-bottom:1px solid #ccd2dc}.point{width:5px;min-height:2px;background:#546}.results h3{margin-top:1.6rem}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:.5rem;border-bottom:1px solid #e1e5ec;font-size:.8rem}.empty{padding:1rem}@media(max-width:800px){.metrics{grid-template-columns:repeat(2,1fr)}}@media(max-width:560px){header,form{display:block}label{margin-bottom:.8rem}.metrics{grid-template-columns:1fr}}`]})
export class BacktestComponent{private readonly api=inject(TradingApiService);symbol='INFY';interval='1d';start='2026-01-01T00:00';end='2026-02-01T00:00';startingCash=100000;quantity=10;feeRate=.001;slippage=5;message='';loading=false;result:BacktestResponse|null=null;submit(event:Event){event.preventDefault();this.loading=true;this.result=null;this.message='Validating historical dataset and simulation assumptions…';const request:BacktestRequest={symbol:{value:this.symbol.trim().toUpperCase()},interval:this.interval.trim(),start:new Date(this.start).toISOString(),end:new Date(this.end).toISOString(),configuration:{startingCash:this.startingCash,quantityPerTrade:this.quantity,feeRate:this.feeRate,slippageBasisPoints:this.slippage,strategyVersion:'baseline-v1'}};this.api.backtest(request).subscribe({next:r=>{this.result=r;this.message=`${r.status}: ${r.simulationLabel}`;this.loading=false},error:e=>{this.message=e?.error?.message??'Backtest failed. Historical data or configuration must be corrected.';this.loading=false}})}curveHeight(equity:number){const values=this.result?.result.equityCurve.map(p=>p.equity)??[equity];const min=Math.min(...values),max=Math.max(...values);return max===min?70:10+140*(equity-min)/(max-min)}}
