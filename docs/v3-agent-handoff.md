# V3 Agent Handoff

BA: V3.1 starts with dashboard shell and safe API visibility.

Senior Engineer: prefer Angular typed services and keep domain/risk logic server-side.

Trader/Safety: UI must clearly remain paper-only; AI must remain advisory; blocked risk decisions cannot execute.

Next implementation: Angular shell + typed API client + routing + baseline tests.
