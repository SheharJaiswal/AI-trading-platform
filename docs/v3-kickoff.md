# V3 Kickoff

## First vertical slice

Build a responsive Angular dashboard that loads `/health`, `/api/portfolio`, `/api/alerts`, quote and recommendation data, and presents a safe paper-trade confirmation path.

## Done means

The dashboard is runnable, typed, tested, visibly paper-only, and cannot bypass server-side risk authorization.
